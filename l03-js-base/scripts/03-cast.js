/**
 * ПРЕОБРАЗОВАНИЕ ТИПОВ
 *
 * Есть случаи, когда нам нужно явно преобразовать значение в ожидаемый
 * тип.
 */

// Строковое преобразование

let value = 10;
value = String(value);

// Численное преобразование

let n = "2";
n = Number(n);

